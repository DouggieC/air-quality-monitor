FROM python:3.12-slim

WORKDIR /app
RUN mkdir -p /app/data

COPY pyproject.toml .
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ src/
RUN pip install --no-cache-dir .

COPY config/ config/

CMD ["python", "-m", "air_quality_monitor.main"]
