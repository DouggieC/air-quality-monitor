[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "air_quality_monitor"
version = "0.1.0"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
